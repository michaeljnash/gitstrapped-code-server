# ─────────────────────────────────────────────────────────────────────────────
# Timezone & IDs
# ─────────────────────────────────────────────────────────────────────────────
TZ=America/Toronto            # Container timezone (IANA format)
PUID=1000                     # Host user ID that should own files in /config
PGID=1000                     # Host group ID that should own files in /config

# ─────────────────────────────────────────────────────────────────────────────
# Authentication (initial password & optional in-UI change)
# ─────────────────────────────────────────────────────────────────────────────
DEFAULT_PASSWORD=change-me    # Optional. Sets the INITIAL code-server password once (argon2-hashed to FILE__HASHED_PASSWORD).
                              # After first boot, update the password from inside code-server by running the
                              # "Gitstrap code-server" task (keybinding: ctrl+alt+g) and entering a new password.

# ─────────────────────────────────────────────────────────────────────────────
# GitHub bootstrap: two ways to use it
#   A) ENV-DRIVEN (autorun at container start):
#      Set BOTH GH_USERNAME and GH_PAT to run gitstrap automatically on first boot.
#   B) INTERACTIVE (inside code-server):
#      Leave envs blank and press ctrl+alt+g to run "Gitstrap code-server" task; you’ll be prompted for values.
# ─────────────────────────────────────────────────────────────────────────────

# Primary vars used by gitstrap.sh
GH_USERNAME=                  # GitHub username for gitstrap (required if doing ENV-DRIVEN autorun).
GH_PAT=                       # GitHub Classic PAT with scopes: user:email, admin:public_key (required for autorun).
                              # If left blank, you'll be prompted inside code-server when running the task.

# Identity overrides (optional). If not set, gitstrap resolves sensible defaults:
GIT_NAME=                     # Commit name; defaults to GH_USERNAME if empty.
GIT_EMAIL=                    # Commit email; auto-resolved via GitHub API if empty.

# Optional SSH key title to appear in your GitHub account after gitstrap uploads the key:
GH_KEY_TITLE=gistrapped-code-server SSH Key

# ─────────────────────────────────────────────────────────────────────────────
# Repositories to clone & update behavior
# ─────────────────────────────────────────────────────────────────────────────
# Comma-separated list. Formats accepted:
#   owner/repo
#   owner/repo#branch
#   https://github.com/owner/repo (will be normalized to SSH)
GH_REPOS="owner1/repo1#main, owner2/repo2, https://github.com/owner3/repo3"

# If a repo already exists in the workspace:
#   true  → (DEFAULT) fetch/pull (or hard reset to the specified branch)
#   false → skip pulling existing clones
PULL_EXISTING_REPOS=true

# ─────────────────────────────────────────────────────────────────────────────
# Workspace location
# ─────────────────────────────────────────────────────────────────────────────
# Base directory where gitstrap clones repositories
GIT_BASE_DIR=/config/workspace
