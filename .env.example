# ─────────────────────────────────────────────────────────────────────────────
# Timezone & IDs
# ─────────────────────────────────────────────────────────────────────────────
TZ=America/Toronto            # Container timezone (IANA format)
PUID=1000                     # Host user ID that should own files in /config
PGID=1000                     # Host group ID that should own files in /config

# ─────────────────────────────────────────────────────────────────────────────
# Authentication (initial password + later changes via CLI)
# ─────────────────────────────────────────────────────────────────────────────
DEFAULT_PASSWORD=change-me    # Optional. Sets the INITIAL code-server password once (argon2-hashed).
                              # Change later from inside code-server terminal with: `gitstrap passwd`

# ─────────────────────────────────────────────────────────────────────────────
# GitHub bootstrap: two ways to use it
#   A) ENV-DRIVEN (autorun at container start):
#      Set BOTH GH_USERNAME and GH_PAT to auto-run gitstrap on first boot.
#   B) INTERACTIVE (inside code-server terminal):
#      Leave envs blank and run `gitstrap` to be prompted for everything,
#      or run `gitstrap --env` to consume env vars only (no prompts).
# ─────────────────────────────────────────────────────────────────────────────
GH_USERNAME=                  # GitHub username (required for autorun / --env)
GH_PAT=                       # GitHub Classic PAT with scopes: user:email, admin:public_key

# Identity overrides (optional). If not set, gitstrap uses sensible defaults:
GIT_NAME=                     # Commit name; defaults to GH_USERNAME if empty
GIT_EMAIL=                    # Commit email; auto-resolved via GitHub API if empty

# Optional SSH key title that will appear in your GitHub account:
GH_KEY_TITLE=gitstrapped-code-server SSH Key

# ─────────────────────────────────────────────────────────────────────────────
# Repositories to clone & update behavior
# ─────────────────────────────────────────────────────────────────────────────
# Comma-separated list. Formats accepted:
#   owner/repo
#   owner/repo#branch
#   https://github.com/owner/repo (normalized to SSH)
GH_REPOS="owner1/repo1#main, owner2/repo2, https://github.com/owner3/repo3"

# If a repo already exists in the workspace:
#   true  → (DEFAULT) fetch/pull (or reset to specified branch)
#   false → leave existing clones untouched
PULL_EXISTING_REPOS=true

# ─────────────────────────────────────────────────────────────────────────────
# Workspace location
# ─────────────────────────────────────────────────────────────────────────────
# Base directory where gitstrap clones repositories
GIT_BASE_DIR=/config/workspace
